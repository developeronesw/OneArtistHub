function json(data,status=200){return new Response(JSON.stringify(data),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store'}})}
const clean=(v,n=500)=>String(v??'').replace(/\0/g,'').slice(0,n)
async function paypalAccess(env){const base=env.PAYPAL_ENV==='live'?'https://api-m.paypal.com':'https://api-m.sandbox.paypal.com';const token=btoa(`${env.PAYPAL_PARTNER_CLIENT_ID}:${env.PAYPAL_PARTNER_SECRET}`);const r=await fetch(base+'/v1/oauth2/token',{method:'POST',headers:{authorization:`Basic ${token}`,'content-type':'application/x-www-form-urlencoded'},body:'grant_type=client_credentials'});if(!r.ok)throw new Error('PayPal partner authentication failed.');const j=await r.json();return {base,token:j.access_token}}
function authorized(req,env){return !!env.CONNECT_SHARED_SECRET&&req.headers.get('authorization')===`Bearer ${env.CONNECT_SHARED_SECRET}`}
export default {async fetch(req,env){try{const url=new URL(req.url);if(url.pathname==='/health')return json({ok:true,service:'OneArtist Connect',paypalEnvironment:env.PAYPAL_ENV==='live'?'live':'sandbox'});if(!authorized(req,env))return json({ok:false,error:'Unauthorized'},401);if(req.method==='POST'&&url.pathname==='/onboard/start'){const b=await req.json();const trackingId=clean(b.trackingId,120),returnUrl=clean(b.returnUrl,1000);if(!trackingId||!/^https:\/\//i.test(returnUrl))return json({ok:false,error:'trackingId and HTTPS returnUrl are required.'},400);const pp=await paypalAccess(env);const payload={tracking_id:trackingId,partner_config_override:{return_url:returnUrl,return_url_description:'Return to OneArtist Hub'},operations:[{operation:'API_INTEGRATION',api_integration_preference:{rest_api_integration:{integration_method:'PAYPAL',integration_type:'THIRD_PARTY',third_party_details:{features:['PAYMENT','REFUND']}}}}],products:['EXPRESS_CHECKOUT'],legal_consents:[{type:'SHARE_DATA_CONSENT',granted:true}]};const headers={authorization:`Bearer ${pp.token}`,'content-type':'application/json'};if(env.PAYPAL_PARTNER_ATTRIBUTION_ID)headers['PayPal-Partner-Attribution-Id']=env.PAYPAL_PARTNER_ATTRIBUTION_ID;const r=await fetch(pp.base+'/v2/customer/partner-referrals',{method:'POST',headers,body:JSON.stringify(payload)});const j=await r.json();if(!r.ok)return json({ok:false,error:clean(j.message||'PayPal partner referral failed.',500),details:j.details||[]},r.status);const actionUrl=(j.links||[]).find(x=>x.rel==='action_url')?.href,self=(j.links||[]).find(x=>x.rel==='self')?.href;if(!actionUrl)return json({ok:false,error:'PayPal did not return an onboarding URL.'},502);return json({ok:true,actionUrl,self,trackingId},201)}if(req.method==='POST'&&url.pathname==='/onboard/status'){const b=await req.json(),merchantId=clean(b.merchantId,40),trackingId=clean(b.trackingId,120);if(!merchantId||!trackingId||!env.PAYPAL_PARTNER_ID)return json({ok:false,error:'merchantId, trackingId and PAYPAL_PARTNER_ID are required.'},400);const pp=await paypalAccess(env),headers={authorization:`Bearer ${pp.token}`,'content-type':'application/json'};if(env.PAYPAL_PARTNER_ATTRIBUTION_ID)headers['PayPal-Partner-Attribution-Id']=env.PAYPAL_PARTNER_ATTRIBUTION_ID;const r=await fetch(`${pp.base}/v1/customer/partners/${encodeURIComponent(env.PAYPAL_PARTNER_ID)}/merchant-integrations/${encodeURIComponent(merchantId)}`,{headers});const j=await r.json();if(!r.ok)return json({ok:false,error:clean(j.message||'Unable to verify merchant onboarding.',500)},r.status);return json({ok:true,merchantId:j.merchant_id||merchantId,trackingId:j.tracking_id||trackingId,paymentsReceivable:!!j.payments_receivable,primaryEmailConfirmed:!!j.primary_email_confirmed,products:j.products||[],oauthIntegrations:j.oauth_integrations||[]})}return json({ok:false,error:'Not found'},404)}catch(e){return json({ok:false,error:clean(e?.message||e,500)},500)}}}
