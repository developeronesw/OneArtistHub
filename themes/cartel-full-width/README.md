# Cartel Full Width — Official Theme Package

This directory documents the integrated `cartel` theme. The current OneArtistHub architecture keeps public themes in `src/app.js` and `src/styles.css`, so this package is a manifest/documentation companion rather than a separate runtime plugin.

For a future true theme-upload system, this manifest can become the basis for a theme loader.
